<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="action_ojt_certificate" model="ir.actions.act_window">
        <field name="name">Certificates</field>
        <field name="res_model">ojt.certificate</field>
        <field name="view_mode">list,form</field>
    </record>
    <menuitem id="menu_ojt_certificate" name="Certificates"
              parent="menu_ojt_root" action="action_ojt_certificate" sequence="40"/>

    <record id="view_ojt_certificate_list" model="ir.ui.view">
        <field name="name">ojt.certificate.list</field>
        <field name="model">ojt.certificate</field>
        <field name="arch" type="xml">
            <list string="Certificates">
                <field name="name"/>
                <field name="batch_id"/>
                <field name="participant_id"/>
                <field name="partner_id"/>
                <field name="serial_number"/>
                <field name="issued_on"/>
                <field name="grade"/>
                <field name="state"/>
            </list>
        </field>
    </record>

    <record id="view_ojt_certificate_form" model="ir.ui.view">
        <field name="name">ojt.certificate.form</field>
        <field name="model">ojt.certificate</field>
        <field name="arch" type="xml">
            <form string="Certificate">
                <sheet>
                    <header>
                        <field name="state" widget="statusbar" statusbar_visible="draft,issued,revoked"/>
                        <button name="action_issue" type="object" string="Issue"
                                class="btn-primary" invisible="state != 'draft'"/>
                        <button name="action_revoke" type="object" string="Revoke"
                                invisible="state != 'issued'"/>
                        <button name="action_reset_draft" type="object" string="Reset to Draft"
                                invisible="state == 'draft'"/>
                    </header>
                    <group col="2">
                        <group>
                            <field name="name" required="1"/>
                            <field name="batch_id" required="1"/>
                            <field name="participant_id" required="1" domain="[('batch_id', '=', batch_id)]"/>
                            <field name="partner_id" readonly="1"/>
                        </group>
                        <group>
                            <field name="serial_number" readonly="1"/>
                            <field name="qr_token" readonly="1"/>
                            <field name="issued_on"/>
                            <field name="state" readonly="1"/>
                        </group>
                    </group>

                    <group col="2">
                        <group>
                            <field name="attendance_rate" readonly="1"/>
                            <field name="final_score" readonly="1"/>
                            <field name="grade" readonly="1"/>
                        </group>
                        <group>
                            <field name="pdf_report" filename="serial_number"/>
                        </group>
                    </group>

                    <group>
                        <field name="notes"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
</odoo>
